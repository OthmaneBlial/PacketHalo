services:
  server:
    environment:
      PACKETHALO_CONTAINER_LOOPBACK: "0"
      PACKETHALO_CONTROL_TOKEN: ${PACKETHALO_CONTROL_TOKEN:?Set PACKETHALO_CONTROL_TOKEN}
  web:
    environment:
      PACKETHALO_CONTROL_TOKEN: ${PACKETHALO_CONTROL_TOKEN:?Set PACKETHALO_CONTROL_TOKEN}
  simulator:
    environment:
      PACKETHALO_CONTROL_TOKEN: ${PACKETHALO_CONTROL_TOKEN:?Set PACKETHALO_CONTROL_TOKEN}
